require 'rails_helper'

RSpec.describe ApplicationController, type: :controller do
  describe 'includes the current concerns' do
    # it { expect(controller.class.ancestors).to include(Authenticable) }
  end
end
